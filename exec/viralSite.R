#!/usr/bin/env Rscript

library("SamSeq")

# Helper function for getSettings(). Converts arguments to option values,
# validates the options (possibly modifying them in the process), and either
# exits with error or return the resulting list of options.

viralSite()
